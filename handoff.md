# grok-build-web

Public sources live on the `release` branch. Personal/local usage stays on `master` and is not what you publish.

See `README.md` for install and `AGENTS.md` for how the app is put together.
